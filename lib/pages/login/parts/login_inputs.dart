import 'package:flutter/material.dart';

class LoginInputs extends StatelessWidget {
  const LoginInputs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Inputlar');
  }
}
