import 'package:flutter/material.dart';

class SuccessShops extends StatelessWidget {
  const SuccessShops({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Tayyar dukanlar'));
  }
}
