import 'package:flutter/material.dart';

class WaitingShops extends StatelessWidget {
  const WaitingShops({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Garasylyan dukanlar'));
  }
}
