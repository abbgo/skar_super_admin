import 'package:flutter/material.dart';

class CategoryImageInput extends StatelessWidget {
  const CategoryImageInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Expanded(
      child: Row(
        children: [Text('Surat saylan')],
      ),
    );
  }
}
