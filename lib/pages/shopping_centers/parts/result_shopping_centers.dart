import 'package:flutter/material.dart';

class ResultShoppingCenters extends StatelessWidget {
  const ResultShoppingCenters({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Shopping Centers'),
    );
  }
}
